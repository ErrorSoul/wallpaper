%p
  %b Доступность:
  На складе
%p
  %b Тип:
  = @product.type.name
%p
  %b Brand:
  =link_to @product.collection.label.name, label_path(@product.collection.label)
%p
  %b Коллекция:
  =link_to @product.collection.name, collection_path(@product.collection.label)
%p
  %b Размер:
  = "#{@product.weight} x #{@product.height}"
